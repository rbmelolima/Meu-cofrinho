name: meu_cofrinho
description: Gerenciador de cofrinho pessoal

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get:

dev_dependencies:
  flutter_test:
    sdk: flutter
  test:
  mockito:
  faker:
  logger: ^0.9.4

flutter:
  uses-material-design: true

  assets:
    - lib/ui/assets
 
 